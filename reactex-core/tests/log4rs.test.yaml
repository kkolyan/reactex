appenders:
  stdout:
    kind: console
    encoder:
      # https://docs.rs/log4rs/0.11.0/log4rs/encode/pattern/index.html#formatters
      pattern: "{d(%H:%M:%S%.3f)} | {T} | {({l}):5.5} — {m}{n}"
root:
  level: trace
  appenders:
    - stdout
loggers:
  reactex_core:
    level: trace
